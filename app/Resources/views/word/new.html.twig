{% extends 'base.html.twig' %}

{% block main %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="_submit">{{ 'site.new_word.save'|trans }}</button>
    {{ form_end(form) }}

{% endblock main %}
